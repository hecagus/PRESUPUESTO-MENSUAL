import { $ } from './01_consts_utils.js';
import { getState } from './02_data.js';

export const initCharts = () => {
    // (Tu código de gráficas estaba bien, solo corrige el import si es necesario, pero aquí no se usa en admin)
};
