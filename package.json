{
  "name": "hecagus-finance",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "android:web": "node scripts/prepare-capacitor-web.mjs",
    "android:sync": "npm run android:web && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "8.5.1",
    "@capacitor/core": "8.5.1"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.1"
  },
  "engines": {
    "node": ">=20"
  }
}
